
events {
    worker_connections 1024;
}

http {
    # upstream gateway_upstream {
    #     server gateway-api:8080;
    #     keepalive 32;
    #     keepalive_requests 1000;
    #     keepalive_timeout 60s;
    # }

    # upstream keycloak_upstream {
    #     server keycloak:8080;
    #     keepalive 32;
    #     keepalive_requests 1000;
    #     keepalive_timeout 60s;
    # }

    server {
        listen 80;

        location /kibana/ {
            proxy_pass http://10.10.15.49:5601/kibana/;  # << ОБЯЗАТЕЛЬНО /kibana/ в конце!
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        } 
        
        # Route API requests through the Gateway
        # location /api/ {
        #     proxy_pass http://gateway_upstream;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;

        #     proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
        #     proxy_next_upstream_tries 3;
        #     proxy_next_upstream_timeout 10s;
            
        #     proxy_connect_timeout 60s;
        #     proxy_send_timeout 60s;
        #     proxy_read_timeout 60s;
        # } 

        # # Route auth requests directly to Keycloak
        # location /auth/ {
        #     proxy_pass http://keycloak_upstream;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;

        #     proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
        #     proxy_next_upstream_tries 3;
        #     proxy_next_upstream_timeout 10s;
            
        #     proxy_connect_timeout 60s;
        #     proxy_send_timeout 60s;
        #     proxy_read_timeout 60s;
        # }

        # # Default route to Keycloak (for backwards compatibility)
        # location / {
        #     proxy_pass http://keycloak_upstream;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;

        #     proxy_next_upstream error timeout http_500 http_502 http_503 http_504;
        #     proxy_next_upstream_tries 3;
        #     proxy_next_upstream_timeout 10s;
            
        #     proxy_connect_timeout 60s;
        #     proxy_send_timeout 60s;
        #     proxy_read_timeout 60s;
        # }

     location / {
        return 404;
        }
    }

   

    proxy_buffer_size 16k;
    proxy_buffers 4 32k;
    proxy_busy_buffers_size 64k;
}
